const expect = require("chai").expect;
const request = require('request');
const url = "http://localhost:4000/api/twilio"
const mock_data = {};

describe('Twilio to Slack', function() {
  it('Can hit the API endpoint', function() {
    // finish
    }
  })
})
